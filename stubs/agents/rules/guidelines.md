---
trigger: always_on
---

## MCP Tool Utilization
When building or modifying Filament forms, tables, resources, pages, or widgets, you MUST use the `search-examples` MCP tool to find real working implementations from FilamentExamples.com before writing code.